version: '3.8'

services:
  frontend:
    build: .
    container_name: front-container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - BACKEND_URL=http://34.229.217.250:3000
      - FLASK_BACKEND_URL=http://34.229.217.250:3000
      - SECRET_KEY=una_clave_muy_segura
    restart: always
