version: '3.8'

services:
  frontend:
    build: .
    container_name: front-container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - BACKEND_URL=${BACKEND_URL}
      - SECRET_KEY=una_clave_muy_segura
    restart: always
